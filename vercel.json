{
  "framework": "nextjs",
  "buildCommand": "prisma generate && (node scripts/baseline-migrations.js || true) && (prisma migrate deploy || prisma db push --skip-generate) && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 9 * * *"
    }
  ]
}
