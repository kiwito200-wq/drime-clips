{
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/baseline-migrations.js && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 9 * * *"
    }
  ]
}
